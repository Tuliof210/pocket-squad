interface:
  display_name: "Pocket Squad Review"
  short_description: "Review the exact current task head"
  default_prompt: "Use $ps-review to review the current task PR at its exact head SHA."
policy:
  allow_implicit_invocation: true
