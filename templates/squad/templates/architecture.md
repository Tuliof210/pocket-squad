# Architecture

> Canonical description of how the product is built. Working rules for agents and
> contributors belong in `PROTOCOLS.md`.

## System shape

<Short description of the major runtime parts and how data or control moves between them.>

## Stack

- **<Area>** — <technology and version source>.

## Entry points and boundaries

- `<path>` — <responsibility and boundary>.

## Conventions to imitate

- **<Convention>** — imitate `<exemplar path>` because <relevant property>.

## Verified commands

- **Install** — `<exact command>` — declared in `<source>`.
- **Test** — `<exact command>` — declared in `<source>`.
- **Lint/build/typecheck** — `<exact command>` — declared in `<source>`.

## Constraints and protected surfaces

- <Compatibility, security, data, deployment or do-not-touch constraint>.

## Sources and unknowns

- **Source** — `<path>`: <what this source establishes>.
- **Unknown** — <question the repository cannot answer; owner decision required>.
